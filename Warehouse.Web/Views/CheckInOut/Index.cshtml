@{
    ViewData["Title"] = "CheckIn CheckOut Page";
}
<div class="container">
    <div class="text-center">

        <div class="row justify-content-center p-3">
            @Html.ActionLink("CheckOut Item", "ItemsCheckOutList", null, null, new { @class = "btn btn-primary" })
        </div>

        <div class="row justify-content-center p-3">
            @Html.ActionLink("CheckOut Items by Structure", "StructuresList", null, null, new { @class = "btn btn-primary" })
        </div>

        <div class="row justify-content-center p-3">
            @Html.ActionLink("CheckIn Item", "ItemsCheckInList", null, null, new { @class = "btn btn-secondary" })
        </div>

    </div>
</div>